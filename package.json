{ "homepage":"https://sergiomuguruza.github.io/CRUD-OK",
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d crud-ok",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
 }
}
